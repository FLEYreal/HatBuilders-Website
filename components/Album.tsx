import { Box } from "@mui/material";

import { ImageProps } from "next/image";
import { useRouter } from "next/navigation";



export default async function Album({ images }: { images: [] }) {


    return (
        <Box sx={{
            flexFlow: 'row nowrap'
        }}>

        </Box>
    );
};