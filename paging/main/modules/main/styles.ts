'use client'

// Emotion
import { css } from "@emotion/react";

export const BottomPlatform = css`
    position: 'absolute';
    height: '120px';
    bottom: 0;
    left: 0;
    background: linear-gradient(180deg, #121212 0%, #000 100%);
    box-shadow: 0px 3px 0px 0px rgba(255, 255, 255, 0.15) inset, -3px -3px 0px 0px rgba(0, 0, 0, 0.30) inset;
    border: 1px solid white;
`